# RPC-Java 生产级改进方案

## Context

RPC-Java 当前是一个教学级框架，核心链路打通但缺少生产环境必需的治理能力。目标是按互联网大厂（对标 Dubbo/gRPC/美团 MTThrift 等内部框架）的实际开发标准，列出可改进点并排优先级。以下每项均基于对当前代码的精确审查，非泛泛而谈。

---

## 一、配置管理（P0，基础设施）

**现状：** ZK 地址 `127.0.0.1:2181`、端口 `9999`、限流参数 `rate=100/capacity=10`、重试次数 `3` 等全部硬编码在代码中。

**改进：**
- 引入配置文件格式（`properties` 或 YAML），统一在 `src/main/resources/rpc.properties` 中管理：ZK 地址、服务端口、序列化方式、线程池大小、超时时间、限流参数等
- 客户端/服务端启动时加载配置，支持启动参数覆盖（如 `-Drpc.zk.address=10.0.0.1:2181`）
- 可考虑接入 Apollo/Nacos 配置中心实现动态配置推送（大厂标配）

**涉及文件：** 新增配置加载类；`ZKServiceCenter`、`ZKServiceRegister`、`NettyRpcClient`、`NettyRPCRPCServer`、`TokenBucketRateLimitImpl`、`GuavaRetry` 等改为读取配置。

---

## 二、异步调用支持（P0，性能核心）

**现状：** 客户端 `sendRequest` 用 `CountDownLatch` 阻塞等待，每个请求占一个线程直到响应返回。

**改进：**
- `RpcClient` 接口新增 `CompletableFuture<RpcResponse> sendRequestAsync(RpcRequest request)` 方法
- `ClientProxy` 支持异步模式：`getProxy` 返回的代理方法可返回 `CompletableFuture<Object>`
- 服务端方法也可定义为返回 `CompletableFuture`，实现全链路异步
- 这对大厂高并发场景至关重要（如 Dubbo 的 async 调用）

**涉及文件：** `RpcClient`、`NettyRpcClient`、`ClientProxy`、`NettyRPCServerHandler`。

---

## 三、服务治理增强（P1，生产刚需）

### 3.1 服务分组与版本控制
**现状：** 服务名仅用接口全限定名，无版本/分组概念。

**改进：**
- `RpcRequest` 增加 `version` 和 `group` 字段
- `ServiceProvider` 注册时携带版本和分组信息（ZK 路径变为 `/MyRPC/group/version/interfaceName/address`）
- 客户端可指定调用特定版本/分组的服务（灰度发布、AB 测试的基础）

### 3.2 权重与动态调权
**现状：** 负载均衡只支持随机/轮询/一致性哈希，无权重概念。

**改进：**
- 服务注册时可指定权重（ZK 节点 data 存储权重值）
- 负载均衡策略感知权重（加权轮询、加权随机）
- 权重可动态调整实现流量调度

### 3.3 服务优雅下线（Graceful Shutdown）
**现状：** `RpcServer.stop()` 是空实现，直接杀进程会导致在途请求丢失。

**改进：**
- 维护正在处理的请求计数器
- `stop()` 时先关闭 ZK 注册（停止接收新请求），等待在途请求完成（设超时上限），最后关闭 Netty 线程池
- 支持 SIGTERM 信号处理

**涉及文件：** `RpcServer`、`NettyRPCRPCServer`、`ServiceProvider`、`WorkThread`。

---

## 四、可观测性（P1，大厂标配）

**现状：** 仅靠 SLF4J 日志，无结构化监控指标。

**改进：**
- **调用链追踪：** `RpcRequest` 增加 `traceId`（请求唯一标识，贯穿整条调用链），日志统一打印 traceId，便于 ELK/SkyWalking 关联
- **Metrics 指标：** 暴露 QPS、RT（响应时间）、成功率、超时率、熔断次数、限流次数等指标，接入 Prometheus
- **健康检查：** 服务端暴露健康检查端点，注册中心可感知节点健康状态
- **调用日志：** 记录每次调用的请求参数摘要、响应耗时、调用方/被调用方 IP（审计需要）

**涉及文件：** `RpcRequest`、`RpcResponse`、`NettyRPCServerHandler`、`ClientProxy`。

---

## 五、高可用增强（P1）

### 5.1 集群容错策略（Cluster 层）
**现状：** 只有熔断器，无集群级别的容错。

**改进：**
- 引入 Cluster 层，支持多种容错策略：
  - `Failfast`：快速失败，调用失败立即报错（默认）
  - `Failsafe`：失败安全，调用失败返回空值（非核心调用）
  - `Forking`：并行调用多个节点，取第一个响应（实时性要求高的场景）
  - `Broadcast`：广播调用所有节点（如刷新缓存）

### 5.2 重试幂等保障
**现状：** 重试白名单存在，但无幂等键机制。

**改进：**
- 重试时携带幂等标识，服务端可做去重（防重复写入）
- 区分可重试异常（网络超时）和不可重试异常（业务异常）

### 5.3 熔断器增强
**现状：** 熔断器按方法名粒度，但降级策略单一（返回 null）。

**改进：**
- 支持自定义降级逻辑（Fallback）：调用失败时执行预设的降级方法
- 增加滑动窗口统计（替代简单的计数器），更精确地判断熔断阈值
- 增加半开状态下的限流（防止恢复瞬间被打垮）

**涉及文件：** `CircuitBreaker`、`ClientProxy`、`GuavaRetry`、新增 Cluster 接口及实现。

---

## 六、SPI 扩展机制（P2，架构扩展性）

**现状：** 序列化、负载均衡等扩展点是硬编码 switch。

**改进：**
- 引入 Java SPI（`java.util.ServiceLoader`）或自研轻量 SPI
- 每个扩展点（序列化、负载均衡、注册中心、容错策略）对应一个 `META-INF/services/xxx` 配置文件
- 新增实现只需加 jar 包 + 配置，无需改框架代码（开闭原则）
- Dubbo 的 `@SPI` 注解机制是最佳实践参考

**涉及文件：** `Serializer`、`LoadBalance`、`ServiceRegister`、`ServiceCenter` 等接口的加载逻辑。

---

## 七、协议与序列化增强（P2）

### 7.1 协议安全性
**现状：** 自定义协议无校验机制。

**改进：**
- 协议头增加 magic number（魔术字节，如 `0xCAFEBABE`），快速识别非法连接
- 增加 CRC/校验和，防传输过程中数据损坏
- 可选：TLS 加密传输（防中间人攻击）

### 7.2 序列化扩展
**现状：** 仅 Java 原生 + FastJSON。

**改进：**
- 接入 Hessian2（Dubbo 默认，跨语言友好）
- 接入 Protobuf（Google 标准，高性能，强类型）
- 接入 Kryo（高性能 Java 序列化）
- 序列化器自动协商（客户端和服务端握手时协商使用的序列化方式）

---

## 八、线程模型优化（P2）

**现状：** Netty 服务端 `handler` 在 Netty 工作线程中直接做反射调用，阻塞 IO 线程。

**改进：**
- 服务端引入业务线程池，Netty worker 线程只做编解码，业务方法调用提交到业务线程池
- 业务线程池参数可配置（核心线程数、最大线程数、队列容量、拒绝策略）
- 不同接口可分配独立线程池（舱壁模式，防止一个慢接口拖垮整个服务）

**涉及文件：** `NettyRPCServerHandler`、`NettyServerInitializer`。

---

## 九、测试体系（P2）

**现状：** `AppTest` 是空壳，无任何有效测试。

**改进：**
- 单元测试：为每个核心类编写 JUnit 测试（序列化/反序列化、负载均衡、限流器、熔断器、编解码器）
- 集成测试：嵌入式 ZooKeeper（curator-test 提供的 `TestingServer`）+ 真实 Netty 通信的端到端测试
- 性能测试：JMeter 或自研压测工具，跑 QPS/RT 基准
- 混沌测试：模拟网络超时、服务宕机，验证容错机制

**涉及文件：** `pom.xml`（加 junit、curator-test 依赖）、`src/test/` 下新增测试类。

---

## 十、其他工程化改进（P3）

- **pom.xml 完善：** 加 `maven-source-plugin`（发布源码）、`maven-javadoc-plugin`、`maven-gpg-plugin`（发布签名）；加 `distributionManagement` 配置私服地址
- **CI/CD：** GitHub Actions / Jenkins 流水线，自动跑单测、代码检查（SonarQube）、构建发布
- **代码规范：** Checkstyle / SpotBugs 静态扫描；统一异常处理规范
- **文档：** README 补充架构图、快速开始、配置项说明、扩展开发指南
- **跨语言：** 通过 IDL（如 Thrift IDL / Protobuf）定义服务接口，生成多语言 stub
- **服务文档：** 接口文档自动生成（类似 Swagger for RPC）

---

## 优先级总览

| 优先级 | 改进项 | 对标大厂能力 |
|--------|--------|-------------|
| P0 | 配置管理 | 所有框架的基础 |
| P0 | 异步调用 | 高并发必备 |
| P1 | 服务治理（版本/分组/权重/优雅下线） | 灰度发布、流量调度 |
| P1 | 可观测性（traceId/Metrics/日志） | 线上排障必备 |
| P1 | 高可用增强（集群容错/降级/熔断增强） | 稳定性保障 |
| P2 | SPI 扩展机制 | 架构扩展性 |
| P2 | 协议安全 + 序列化扩展 | 安全 + 跨语言 |
| P2 | 线程模型优化 | 性能优化 |
| P2 | 测试体系 | 质量保障 |
| P3 | 工程化（CI/CD/文档/规范） | 团队协作 |

---

## 验证方式

每个改进项完成后：
1. `mvn compile` 编译通过
2. 对应新增的单元测试通过
3. `TestServer` + `TestClient` 端到端验证不回归
4. 涉及配置项的，验证配置加载生效
