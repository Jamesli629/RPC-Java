# RPC-Java 生产级改进报告

## 1. 项目概述

### 1.1 项目是什么

RPC-Java 是一个用 Java 实现的轻量级 RPC（远程过程调用）框架，基于 Netty 4.1.51 + ZooKeeper（Curator 客户端）。框架支持服务注册与发现、负载均衡、限流、熔断、重试等核心能力。

### 1.2 三轮改进的整体目标

从"教学级能跑"演进为"生产级可用"，每轮聚焦不同优先级的能力：

| 轮次 | 优先级 | 核心目标 |
|------|--------|----------|
| 第一轮 | P0 | 基础设施：配置管理、异步调用、服务治理（版本/分组/权重）、优雅下线 |
| 第二轮 | P0+P1 | 协议与稳定性：协议增强（Magic Number/版本号）、业务线程池、异常传播、心跳、ShutdownHook、日志规范 |
| 第三轮 | P1+P2 | 生产级能力：Metrics、健康检查、集群容错、幂等保障、熔断增强、SPI、CRC、Kryo 序列化、测试体系 |

### 1.3 改进覆盖的能力范围

```
┌─────────────────────────────────────────────────────────────┐
│                     RPC-Java 生产级能力矩阵                    │
├──────────────┬──────────────────────────────────────────────┤
│   通信协议    │ Magic Number + 版本号 + CRC32 + 可扩展序列化    │
│   网络通信    │ Netty NIO + 连接复用 + 空闲检测 + 心跳保活      │
│   服务治理    │ 注册发现 + 版本/分组/权重 + 优雅下线 + Shutdown │
│   负载均衡    │ 随机/轮询/一致性哈希/加权轮询（4 种策略）       │
│   流量控制    │ 令牌桶限流 + 半开状态限流                       │
│   集群容错    │ Failfast/Failsafe/Forking/Broadcast（4 种）    │
│   熔断降级    │ 三态熔断 + 滑动窗口 + Fallback 降级             │
│   重试机制    │ Guava Retrying + 白名单 + 幂等保障              │
│   可观测性    │ Micrometer Metrics + 健康检查 HTTP + 调用日志   │
│   扩展性      │ SPI 机制（序列化/负载均衡/容错策略可插拔）       │
│   测试        │ 25 个 JUnit 5 单元测试覆盖核心链路              │
└──────────────┴──────────────────────────────────────────────┘
```

---

## 2. 第一轮改进（P0 基础设施）

### 2.1 配置管理

**做了什么：** 引入 `ConfigManager` 单例 + `rpc.properties` 配置文件，替换 30+ 处硬编码参数。

**为什么这么做：** 原始代码中 ZK 地址、端口、限流参数、重试次数等全部硬编码，每次修改都需要重新编译。生产环境需要外部化配置。

**关键代码：**
```java
// ConfigManager.java - 双重检查锁单例
public static ConfigManager getInstance() {
    if (instance == null) {
        synchronized (ConfigManager.class) {
            if (instance == null) {
                instance = new ConfigManager();
            }
        }
    }
    return instance;
}

// 支持配置中心覆盖（预留 Nacos/Apollo）
public String getString(String key, String defaultValue) {
    if (configCenter != null) {
        String value = configCenter.getProperty(key);
        if (value != null && !value.isEmpty()) return value;
    }
    return properties.getProperty(key, defaultValue);
}
```

### 2.2 异步调用

**做了什么：** `RpcClient` 接口新增 `sendRequestAsync()` 方法，`ClientProxy` 根据返回类型自动选择同步/异步路径。

**为什么这么做：** 原始实现用 `CountDownLatch` 阻塞等待，每个请求占一个线程。高并发下线程资源耗尽。全链路异步是现代 RPC 框架的标配。

**关键代码：**
```java
// ClientProxy.invoke - 自动选择同步/异步
if (method.getReturnType() == CompletableFuture.class) {
    return sendAsync(request, circuitBreaker);  // 异步路径
} else {
    return sendSync(request, circuitBreaker);   // 同步路径（向后兼容）
}

// NettyRpcClient - 同步委托给异步
public RpcResponse sendRequest(RpcRequest request) {
    return sendRequestAsync(request).join();
}
```

**设计决策：同步委托给异步。** 统一实现，同步路径自动获得异步的所有能力（超时、连接复用），避免两套逻辑维护。

### 2.3 服务分组与版本控制

**做了什么：** `RpcRequest` 新增 `version` 和 `group` 字段，ZK 路径从 `/{service}/{address}` 改为 `/{service}/{version}/{group}/{address}`。

**为什么这么做：** 灰度发布、AB 测试、流量隔离是生产环境刚需。没有版本/分组，无法做到无损上线。

### 2.4 权重与动态调权

**做了什么：** 服务注册时携带权重（ZK 节点 data 存储），新增 `WeightedRoundLoadBalance`（Nginx 平滑加权轮询）。

**为什么这么做：** 不同服务器性能不同，需要按权重分配流量。Nginx 平滑加权轮询避免连续请求打到同一节点。

### 2.5 优雅下线

**做了什么：** `NettyRPCRPCServer.stop()` 实现四阶段优雅下线：draining→ZK 注销→等待在途(30s)→关闭资源。

**为什么这么做：** 直接杀进程会导致在途请求丢失。优雅下线确保正在进行中的请求完成后再关闭。

**关键代码：**
```java
public void stop() {
    draining = true;  // ① 拒绝新请求
    serviceProvider.unregisterAll();  // ② ZK 注销
    // ③ 等待在途请求完成（最多 30s）
    while (inFlightRequests.get() > 0 && System.currentTimeMillis() < deadline) {
        Thread.sleep(100);
    }
    // ④ 关闭资源
    bossGroup.shutdownGracefully();
    workGroup.shutdownGracefully();
}
```

---

## 3. 第二轮改进（协议与稳定性）

### 3.1 协议增强（Magic Number + 版本号）

**做了什么：** 帧头新增 Magic Number (`0xCAFE`) + 协议版本 (`0x0001`)。

**为什么这么做：** 没有 Magic Number，非法连接（如 HTTP 探测）会导致解码异常，甚至可能攻击服务。没有版本号，协议无法向前演进。

**关键代码：**
```java
// MyDecoder - 首先校验 Magic Number
short magicNumber = in.readShort();
if (magicNumber != ProtocolConstants.MAGIC_NUMBER) {
    logger.warn("非法协议连接，Magic Number=0x{}", Integer.toHexString(magicNumber & 0xFFFF));
    channelHandlerContext.close();  // 立即关闭非法连接
    return;
}
```

**帧格式变更：**
```
原：messageType(2) + serializerType(2) + length(4) + channelId(4) = 12 字节
新：magicNumber(2) + version(2) + messageType(2) + serializerType(2) + length(4) + channelId(4) = 16 字节
```

### 3.2 服务端业务线程池

**做了什么：** 新增 `BusinessThreadPoolHandler`，将反射调用从 Netty IO 线程卸载到独立业务线程池。

**为什么这么做：** 原始实现直接在 Netty IO 线程做反射调用。如果某个接口耗时慢（如数据库查询），会阻塞整个 IO 线程，导致所有连接都无法处理。这是性能杀手。

**关键代码：**
```java
// BusinessThreadPoolHandler.java
public void channelRead(ChannelHandlerContext ctx, Object msg) {
    BUSINESS_EXECUTOR.execute(() -> {
        ctx.fireChannelRead(msg);  // 在业务线程中执行后续 Handler
    });
}

// 队列满时的背压策略
new ThreadPoolExecutor.CallerRunsPolicy();  // IO 线程直接执行
```

**设计模式：舱壁模式（Bulkhead）。** 业务线程池与 IO 线程隔离，慢接口只影响业务线程池，不会阻塞网络吞吐。

### 3.3 异常信息传播

**做了什么：** `RpcResponse` 新增 `exceptionClass`/`exceptionMessage` 字段，服务端反射异常时封装原始异常信息，客户端日志 warn 记录。

**为什么这么做：** 原始实现只返回 `code=500 + "服务器发生错误"`，客户端完全不知道服务端出了什么问题，排障极其困难。

**关键代码：**
```java
// RpcResponse.java - 携带异常信息的失败响应
public static RpcResponse failWithException(Throwable e) {
    RpcResponse resp = new RpcResponse();
    resp.setCode(500);
    resp.setMessage("服务器发生错误: " + e.getClass().getName() + ": " + e.getMessage());
    resp.setExceptionClass(e.getClass().getName());
    resp.setExceptionMessage(e.getMessage());
    return resp;
}

// NettyRPCServerHandler - 分离 InvocationTargetException
} catch (InvocationTargetException e) {
    Throwable target = e.getTargetException();  // 获取业务真实异常
    RpcResponse response = RpcResponse.failWithException(target);
    response.setChannelId(rpcRequest.getChannelId());
    return response;
}
```

### 3.4 心跳保活

**做了什么：** 客户端新增 `IdleStateHandler(15s)` + `ClientHeartbeatHandler`，检测连接断开时自动清理 channelCache。

**为什么这么做：** 只有服务端有 `IdleStateHandler(30s)`，客户端无法及时感知连接断开。断开的连接会一直留在 channelCache 中，后续请求会发到死连接直到超时。

**关键代码：**
```java
// ClientHeartbeatHandler.java
@Override
public void channelInactive(ChannelHandlerContext ctx) {
    logger.warn("客户端通道关闭，远程地址: {}", ctx.channel().remoteAddress());
    rpcClient.handleChannelInactive(ctx.channel().remoteAddress().toString());
}
```

### 3.5 ShutdownHook

**做了什么：** `NettyRPCRPCServer.start()` 中注册 JVM 关闭钩子，SIGTERM/SIGINT 时自动触发优雅下线。

**为什么这么做：** 生产环境常用 `kill -15` 停止进程。没有 ShutdownHook，直接杀进程会导致 ZK 节点残留、在途请求丢失。

### 3.6 日志规范化

**做了什么：** rootLogger 从 ERROR 改为 INFO，核心框架所有 `System.out.println` 替换为 SLF4J。

**为什么做什么：** ERROR 级别导致 INFO/WARN 日志不可见，线上排障缺少关键信息。`System.out.println` 无法控制级别、无法输出到文件。

---

## 4. 第三轮改进（P1 + P2 生产级能力）

### 4.1 Metrics 指标采集（Micrometer）

**做了什么：** 引入 Micrometer 作为指标门面，采集服务端/客户端请求耗时（Timer + P50/P90/P99）、错误计数、超时计数、限流拒绝、熔断器状态。

**为什么这么做：** 线上排障完全依赖日志效率太低。结构化指标可以接入 Prometheus + Grafana，实时查看 QPS/RT/错误率。

**关键代码：**
```java
// RpcMetrics.java
public static void recordServerRequest(String interfaceName, String methodName,
                                        long durationMs, boolean success) {
    Timer.builder("rpc.server.requests")
        .tag("interface", interfaceName)
        .tag("method", methodName)
        .tag("result", success ? "success" : "error")
        .publishPercentiles(0.5, 0.9, 0.99)
        .register(REGISTRY)
        .record(durationMs, TimeUnit.MILLISECONDS);
}

// 暴露 MeterRegistry 供接入方绑定 Prometheus
public static MeterRegistry getRegistry() { return REGISTRY; }
```

**使用方式：**
```java
// 接入 Prometheus（需添加 micrometer-registry-prometheus dependency）
PrometheusMeterRegistry prometheus = new PrometheusMeterRegistry(PrometheusConfig.defaultConfig());
Metrics.getRegistry().add(prometheus);
// 然后暴露 /metrics 端点供 Prometheus 抓取
```

### 4.2 健康检查 HTTP 端点

**做了什么：** 新增 `HealthcheckServer`，提供 `/health`（Liveness）和 `/ready`（Readiness）HTTP 端点。

**为什么这么做：** Kubernetes 需要 Liveness/Readiness Probe 来判断容器健康状态。没有健康检查，K8s 无法自动重启不健康的 Pod，也无法控制流量路由。

**关键代码：**
```java
// HealthcheckServer.java - 基于 Netty 的轻量 HTTP 服务
ch.pipeline()
    .addLast(new HttpResponseEncoder())
    .addLast(new HttpRequestDecoder(4096, 8192, 8192, false))
    .addLast(new HttpObjectAggregator(65536))
    .addLast(new HealthcheckHandler(readinessChecker));

// Readiness 判断逻辑
if ("/ready".equals(uri)) {
    if (!running || draining) return HealthStatus.DOWN;
    return HealthStatus.UP;
}
```

**返回格式：**
- `200 OK` + `{"status":"UP"}` — 服务正常
- `503 Service Unavailable` + `{"status":"DOWN"}` — 服务不可用

### 4.3 调用日志增强

**做了什么：** 服务端 Handler 记录接口名、方法名、耗时、调用方 IP、成功/失败结果。

**为什么这么做：** 线上出问题时，需要快速定位是哪个接口慢、哪个调用方有问题、RT 多少。结构化日志便于 ELK 聚合分析。

**关键代码：**
```java
// NettyRPCServerHandler.java
private void logInvocation(RpcRequest request, RpcResponse response, long duration,
                            ChannelHandlerContext ctx) {
    String remoteAddr = ctx.channel().remoteAddress().toString();
    if (response.getCode() == 200) {
        logger.info("调用成功: {}.{} | 耗时: {}ms | 调用方: {}",
                request.getInterfaceName(), request.getMethodName(), duration, remoteAddr);
    } else {
        logger.warn("调用失败: {}.{} | code={} | 耗时: {}ms | 调用方: {} | {}",
                request.getInterfaceName(), request.getMethodName(),
                response.getCode(), duration, remoteAddr, response.getMessage());
    }
}
```

### 4.4 集群容错策略

**做了什么：** 引入 Cluster 层，支持 Failfast（快速失败）、Failsafe（失败安全）、Forking（并行调用）、Broadcast（广播调用）四种策略。

**为什么这么做：** 不同场景需要不同容错策略。写操作需要 Failfast 快速失败，日志上报可以 Failsafe 忽略失败，实时性高的读操作用 Forking 取最快响应，缓存刷新用 Broadcast 通知所有节点。

**关键代码：**
```java
// Cluster 接口
public interface Cluster {
    RpcResponse invoke(RpcRequest request, List<String> addressList) throws Exception;
    String name();
}

// ClusterInvoker - 策略入口（通过配置切换）
String strategy = ConfigManager.getInstance()
    .getString("rpc.client.cluster.strategy", "failfast");
switch (strategy.toLowerCase()) {
    case "failsafe":  cluster = new FailsafeCluster(rpcClient); break;
    case "forking":   cluster = new ForkingCluster(rpcClient); break;
    case "broadcast": cluster = new BroadcastCluster(rpcClient); break;
    default:          cluster = new FailfastCluster(rpcClient); break;
}

// ForkingCluster - 并行调用取最快
CompletableFuture<RpcResponse>[] futures = new CompletableFuture[addressList.size()];
for (int i = 0; i < addressList.size(); i++) {
    futures[i] = CompletableFuture.supplyAsync(() -> rpcClient.sendRequest(request), executor);
}
for (CompletableFuture<RpcResponse> future : futures) {
    RpcResponse response = future.get(5, TimeUnit.SECONDS);
    if (response != null && response.getCode() == 200) return response;
}
```

**设计模式：策略模式。** 符合开闭原则，新增策略只需实现 Cluster 接口，无需修改 ClientProxy。

### 4.5 重试幂等保障

**做了什么：** 新增 `IdempotencyChecker`，客户端生成 UUID 幂等键，服务端缓存首次调用结果，重复请求返回缓存。

**为什么这么做：** 网络超时触发重试时，请求可能已经到达服务端并执行，重试会导致重复操作（如重复扣款）。幂等保障确保重试安全。

**关键代码：**
```java
// IdempotencyChecker.java
public Object checkAndRecord(String key, Object response) {
    CacheEntry existing = cache.get(key);
    if (existing != null && !existing.isExpired()) {
        return existing.response;  // 重复请求，返回缓存结果
    }
    if (response != null) {
        cache.put(key, new CacheEntry(response, ttlMs));
    }
    return null;
}

// 定时清理过期条目（每分钟）
cleaner.scheduleAtFixedRate(this::evictExpired, 1, 1, TimeUnit.MINUTES);

// ClientProxy - 生成幂等键
RpcRequest request = RpcRequest.builder()
        .idempotencyKey(UUID.randomUUID().toString())
        .build();
```

**设计决策：幂等键由客户端生成。** 服务端无需理解业务语义，通用性强。

### 4.6 熔断器增强

**做了什么：** 滑动窗口替代简单计数器、半开状态限流、Fallback 降级返回类型安全默认值。

**为什么这么做：** 简单计数器时间维度不精确，半开状态没有限流会导致恢复瞬间被打垮。

**关键代码：**
```java
// 滑动窗口
private final Queue<Boolean> slidingWindow;
public synchronized void recordFailure() {
    addWindowResult(false);
    if (getWindowFailureCount() >= failureThreshold) {
        state = CircuitBreakerState.OPEN;
    }
}

// 半开状态限流
case HALF_OPEN:
    int current = halfOpenRequests.incrementAndGet();
    return current <= halfOpenMaxRequests;

// Fallback 降级
private Object handleFallback(Method method) {
    Class<?> returnType = method.getReturnType();
    if (returnType == int.class) return 0;
    else if (returnType == boolean.class) return false;
    return null;
}
```

### 4.7 SPI 扩展机制

**做了什么：** 新增 `SpiLoader`，基于 Java `ServiceLoader` 实现可插拔扩展。

**为什么这么做：** 原始硬编码 switch 选择序列化器，新增方式需修改框架代码。SPI 让扩展只需"加 jar 包 + 配置文件"。

**关键代码：**
```java
// SpiLoader.java
public static <T> Map<String, T> load(Class<T> service) {
    ServiceLoader<T> loader = ServiceLoader.load(service);
    for (T impl : loader) {
        String name = impl.getClass().getSimpleName()
                .replaceAll("(Impl|Serializer)$", "");
        implMap.put(name, impl);
    }
    return implMap;
}

// Serializer.java - 优先查 SPI，兜底内置
static Serializer getSerializerByCode(int code) {
    for (Serializer impl : SpiLoader.load(Serializer.class).values()) {
        if (impl.getType() == code) return impl;
    }
    switch (code) { case 0: return new ObjectSerializer(); ... }
}
```

### 4.8 CRC32 校验和

**做了什么：** 协议帧尾部追加 CRC32 校验和（4 字节），防止传输过程中数据损坏。

**关键代码：**
```java
// 编码
CRC32 crc32 = new CRC32();
crc32.update(serializeBytes);
out.writeInt((int) crc32.getValue());

// 校验
int receivedCrc = in.readInt();
if (receivedCrc != (int) crc32.getValue()) {
    logger.warn("CRC 校验失败");
    return;
}
```

### 4.9 序列化扩展（Kryo）

**做了什么：** 新增 `KryoSerializer`，基于 Kryo 5.5.0 高性能序列化。

**关键代码：**
```java
private static final ThreadLocal<Kryo> KRYO_THREAD_LOCAL = ThreadLocal.withInitial(() -> {
    Kryo kryo = new Kryo();
    kryo.register(RpcRequest.class);
    kryo.register(RpcResponse.class);
    kryo.setRegistrationRequired(false);
    kryo.setReferences(true);
    return kryo;
});

public int getType() { return 3; }  // type=3 代表 Kryo
```

### 4.10 测试体系

**做了什么：** 新增 5 个 JUnit 5 测试类，25 个测试全部通过。

| 测试类 | 覆盖内容 | 测试数 |
|--------|----------|--------|
| SerializerTest | JSON/Java/Kryo 序列化、SPI 加载 | 7 |
| LoadBalanceTest | 随机/轮询/一致性哈希、节点增删 | 4 |
| CircuitBreakerTest | 状态转换、半开限流、失败率 | 7 |
| TokenBucketRateLimitTest | 令牌桶容量、恢复、上限 | 3 |
| CodecTest | 编解码、Magic Number 校验、CRC 校验 | 4 |

---

## 5. 架构演进总结

### 5.1 从教学级到生产级的关键跨越

| 维度 | 教学级（改进前） | 生产级（改进后） |
|------|------------------|------------------|
| **配置** | 硬编码 | 外部化 + 配置中心预留 |
| **协议** | 无校验 | Magic Number + 版本 + CRC32 |
| **线程模型** | IO 线程做业务 | IO 线程只做编解码 + 独立业务线程池 |
| **服务治理** | 无版本/分组/权重 | 版本 + 分组 + 权重 + 优雅下线 + Shutdown |
| **高可用** | 简单熔断器（计数器） | 滑动窗口熔断 + 半开限流 + 4 种集群容错 + 幂等 |
| **可观测性** | 仅日志 | Micrometer Metrics + 健康检查 HTTP + 结构化日志 |
| **扩展性** | 硬编码 switch | SPI 机制可插拔 |
| **测试** | 空壳 | 25 个 JUnit 5 单元测试 |

### 5.2 设计模式和最佳实践

1. **策略模式** — Cluster/LoadBalance 接口 + 多种实现，运行时通过配置切换
2. **舱壁模式** — BusinessThreadPoolHandler 隔离业务与 IO 线程
3. **断路器模式** — 三态转换 + 半开限流防止恢复瞬间被打垮
4. **背压模式** — CallerRunsPolicy 队列满时自然降低接收速率
5. **门面模式** — ClusterInvoker/SpiLoader 统一入口隐藏细节
6. **模板方法** — ServiceCenter/ServiceRegister 的 default 方法委托

### 5.3 后续可继续完善的方向

| 优先级 | 方向 | 说明 |
|--------|------|------|
| P1 | 集成测试 | curator-test TestingServer 做端到端测试 |
| P1 | Prometheus 导出 | micrometer-registry-prometheus + /metrics 端点 |
| P1 | 超时传递 | 客户端设置超时，服务端感知剩余时间 |
| P2 | TLS 加密 | SSL/TLS 支持 |
| P2 | Protobuf 序列化 | 跨语言、强类型 |
| P2 | 配置中心接入 | 实现 ConfigCenter 接口接入 Nacos/Apollo |
| P2 | 链路追踪 | traceId 透接 SkyWalking/Jaeger |
| P3 | CI/CD | GitHub Actions 自动跑单测、代码检查 |
| P3 | 混沌测试 | 模拟网络超时、服务宕机 |

### 5.4 端到端调用链路总览

```
客户端接口调用
    ↓
ClientProxy.invoke（构建请求 → 生成幂等键 → 熔断判断 → 同步/异步选择）
    ↓
GuavaRetry.sendServiceWithRetry（白名单检查 → 重试包装）
    ↓
NettyRpcClient.sendRequestAsync（生成 channelId → 设置超时 → 获取连接 → 写出）
    ↓
    ═══════ 网络传输（Magic + Version + Type + Length + ChannelId + Payload + CRC32）═══════
    ↓
NettyServerInitializer 链路
    ├── IdleStateHandler(30s) → 读空闲检测
    ├── MyDecoder → Magic 校验 → 版本校验 → CRC 校验 → 反序列化
    ├── BusinessThreadPoolHandler → 提交到业务线程池
    └── NettyRPCServerHandler.channelRead0
            ├── 排空状态检查（下线中返回 503）
            ├── 幂等检查（重复请求返回缓存）
            ├── 限流检查（令牌桶）
            ├── 反射调用服务方法
            ├── 异常封装（failWithException）
            ├── 幂等记录（缓存结果）
            ├── 日志记录（接口/方法/耗时/调用方）
            └── Metrics 记录（Timer + Counter）
    ↓
NettyClientHandler.channelRead0 → 根据 channelId 完成 Future
    ↓
ClientProxy 响应处理 → 上报熔断器 → 记录客户端 Metrics → 返回结果
```

---

**总结：** 经过三轮系统性改进，RPC-Java 已从教学级框架演进为具备生产级治理能力的 RPC 框架。后续可继续完善集成测试、Prometheus 导出、TLS 加密、配置中心接入、链路追踪等方向，向更完整的云原生 RPC 框架演进。
